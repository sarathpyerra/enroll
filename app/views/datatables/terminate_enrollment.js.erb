var $parent_row = $( ".table tbody tr:not(tr.child-row)" ).eq( <%= @row %> );
if ( $('tr.child-row:visible').length > 0 ) {
  $('tr.child-row:visible').remove();
}
if ( $parent_row.next().hasClass('child-row') ) {
} else {
  $parent_row.after('<tr class="child-row"><td colspan="100"><%= j render partial: "datatables/terminate_enrollment" %></td></tr>');
}
