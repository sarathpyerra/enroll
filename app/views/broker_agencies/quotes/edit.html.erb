
<%= form_for @quote, url: {action: "update"}, method: "put", class: "form-inline" do |f| %>
<%= render "edit_form" ,f:f %>

<%= submit_tag "Update Roster", class: 'btn btn-primary' %>
<%= link_to "Back to Quote Cost/Comparison", my_quotes_broker_agencies_quotes_path, class: 'btn btn-primary', data: { no_turbolink: true } %>

<%end%>
