
<div class="container-fluid">
<!--<p> Select a roster/quote then focus your search for plans by feature or cost:
	<ul>
		<li>selection options</li>
		<li> limit maximum allowable deductible</li>
		<li> limit by roster cost (based on relationship benefits)</li>
    </ul>
</p>-->
	<input type="hidden" id="quote_id" value="<%= params[:id] %>">
	<div class='row-fluid'>
		<h4>Step 1 - Select the Quote - Benefit Group to compare costs or plan information</h4>
	</div>
	<div class='row-fluid'>
	  <%=	select_tag "benefit_group_select", options_from_collection_for_select(@benefit_groups, "id", "title", @q.try(:id).try(:to_s)), { } %>
	</div>
	<div class='row-fluid'>
	  <h4> Step 2 - Define employer contribution percentages</h4>
	</div>
	<div class='row-fluid'>
	  <%= render partial: 'benefit_sliders'%>
	</div>
	<div class='row-fluid'>
		<h4>Step 3 - Filter your plans / Selection criteria</h4>
	</div>
	<div class='row-fluid'>
		<%= render partial: 'select_plans' %>
	</div>
</div>
